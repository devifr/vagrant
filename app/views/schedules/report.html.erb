<div class="innerLR">
  <% @results.each do |key, data| %>
    <% locals_attributes = { schedule: @schedule, work_form_group: @work_form_group, operator: @operator}.merge(data) %>
    <%= render  partial: "report_#{key}", locals:  locals_attributes %>
  <% end %>
</div>